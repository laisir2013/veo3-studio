# VEO3 環境變量配置模板
# 複製此文件為 .env 並填入實際的值

# ============================================
# 基本配置
# ============================================
NODE_ENV=development
PORT=3000
VITE_APP_ID=veo3-web
# JWT 密鑰（至少 32 字符）
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# ============================================
# 數據庫配置（MySQL/TiDB/PlanetScale）
# ============================================
# 如果不配置數據庫，系統將使用內存存儲（不持久化）
# 內存存儲模式下，部分功能（用戶認證、角色庫、歷史記錄）將受限

# 本地 MySQL 示例：
# DATABASE_URL=mysql://root:password@localhost:3306/veo3_db

# PlanetScale 示例（推薦，免費）：
# DATABASE_URL=mysql://user:pass@aws.connect.psdb.cloud/veo3?ssl={"rejectUnauthorized":true}

# Railway MySQL 示例：
# DATABASE_URL=mysql://root:password@containers-us-west-1.railway.app:6543/railway

DATABASE_URL=

# ============================================
# OAuth 認證配置（Manus 平台）
# ============================================
# 如果不使用用戶認證，可以留空（將使用訪客模式）
OAUTH_SERVER_URL=
OWNER_OPEN_ID=

# ============================================
# VectorEngine API Keys（視頻和圖片生成）
# ============================================
# 至少需要配置 1 個 API Key，最多支持 13 個輪換使用
# 獲取方式：https://vectorengine.ai/

# 必須配置（至少這一個）
VECTOR_ENGINE_API_KEY_1=your-vectorengine-api-key-1

# 可選（提升並發和規避速率限制）
VECTOR_ENGINE_API_KEY_2=
VECTOR_ENGINE_API_KEY_3=
VECTOR_ENGINE_API_KEY_4=
VECTOR_ENGINE_API_KEY_5=
VECTOR_ENGINE_API_KEY_6=
VECTOR_ENGINE_API_KEY_7=
VECTOR_ENGINE_API_KEY_8=
VECTOR_ENGINE_API_KEY_9=
VECTOR_ENGINE_API_KEY_10=
VECTOR_ENGINE_API_KEY_11=
VECTOR_ENGINE_API_KEY_12=
VECTOR_ENGINE_API_KEY_13=

# ============================================
# KreadoAI TTS API（語音生成）
# ============================================
# 用於生成粵語、普通話、英語語音
# 獲取方式：https://www.kreadoai.com/
KREADO_API_KEY=your-kreado-api-key

# ============================================
# LLM API Keys（故事分析和腳本生成）
# ============================================

# OpenAI API（用於 GPT-4, GPT-5）
# 獲取方式：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic API（用於 Claude 模型）
# 獲取方式：https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Google Gemini API（用於 Gemini 模型）
# 獲取方式：https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-gemini-api-key

# ============================================
# AWS S3 存儲配置（媒體文件上傳）
# ============================================
# 用於存儲生成的視頻、圖片、音頻文件
# 如果不配置，可能使用臨時 URL（不持久）

AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# ============================================
# Midjourney 配置（圖片生成）
# ============================================
# 通過 VectorEngine 調用 Midjourney
# 通常使用 VECTOR_ENGINE_API_KEY 即可

# 如果有專用的 Midjourney API Key：
# MIDJOURNEY_API_KEY=your-midjourney-api-key

# ============================================
# 通知服務配置（可選）
# ============================================
# 用於發送生成完成通知給管理員
NOTIFICATION_WEBHOOK_URL=

# ============================================
# 高級配置（通常不需要修改）
# ============================================

# API 請求超時時間（毫秒）
# API_TIMEOUT=300000

# 視頻生成並發數
# MAX_CONCURRENT_VIDEOS=6

# 啟用調試模式
# DEBUG=true

# ============================================
# 配置檢查清單
# ============================================
#
# 最小化配置（可運行但功能受限）：
# ✅ VECTOR_ENGINE_API_KEY_1 - 必須
# ✅ OPENAI_API_KEY 或 ANTHROPIC_API_KEY - 必須（至少一個）
# ✅ KREADO_API_KEY - 必須
#
# 推薦配置（完整功能）：
# ✅ 所有最小化配置
# ✅ DATABASE_URL - 推薦（數據持久化）
# ✅ 多個 VECTOR_ENGINE_API_KEYs - 推薦（提升性能）
# ✅ AWS S3 配置 - 推薦（媒體文件存儲）
#
# 完整配置（生產環境）：
# ✅ 所有推薦配置
# ✅ OAUTH 配置 - 可選（用戶認證）
# ✅ NOTIFICATION_WEBHOOK_URL - 可選（通知服務）
#
# ============================================
# 故障排除
# ============================================
#
# 1. 如果啟動失敗，檢查：
#    - Node.js 版本是否 >= 18
#    - pnpm 是否已安裝
#    - .env 文件是否存在且格式正確
#
# 2. 如果視頻生成失敗，檢查：
#    - VECTOR_ENGINE_API_KEY 是否有效
#    - API Key 是否有足夠的額度
#    - 網絡連接是否正常
#
# 3. 如果語音生成失敗，檢查：
#    - KREADO_API_KEY 是否有效
#    - KreadoAI 服務是否可用
#
# 4. 如果故事分析失敗，檢查：
#    - OPENAI_API_KEY 或 ANTHROPIC_API_KEY 是否有效
#    - LLM 服務是否可用
#
# ============================================
# 獲取幫助
# ============================================
#
# 技術文檔：docs/
# 本地運行指南：LOCAL_SETUP_GUIDE.md
# 分析報告：analysis_report/VEO3_Analysis_Report.md
# 診斷報告：/home/user/VEO3_深度診斷報告.md
#
# ============================================
